{
  "name": "e2e-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Official documentation for E2E Networks.",
  "scripts": {
    "api:build": "npm run api:build:myaccount && npm run api:build:tir",
    "api:build:myaccount": "sh ./static/api/myaccount/merge_and_bundle.sh",
    "api:build:tir": "sh ./static/api/tir/merge_and_bundle.sh",
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@docusaurus/plugin-content-docs": "^3.9.2",
    "@docusaurus/preset-classic": "^3.9.2",
    "@docusaurus/types": "^3.9.2",
    "docusaurus": "^1.14.7",
    "lucide-react": "^1.7.0",
    "prism-react-renderer": "^2.4.1"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
